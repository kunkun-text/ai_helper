# AI-helper 需求清单（2026-09-15 确认）

> 用途：罗列已确认的改进需求，下次直接按此清单开工。**本文档只记录需求，不包含实现。**
> 状态约定：`待开发` → `开发中` → `待实测` → `已完成`
>
> ✅ 2026-09-16 更新：一、二 已完成——答辩页渲染错乱 bug 已根治（根因：fixed 悬浮层渲染合成 + textarea 内部滚动合成，详见 CHANGES.md「2026-09-16」第三节），开发者工具全流程验证通过，**真机待验**。

---

## 一、答辩输入框 → 微信式对话框（P0，前端）✅ 已完成（2026-09-16，工具验证通过，真机待验）

> 定稿与原需求的差异：① `maxlength` 按用户要求放宽至 2000（支持复制粘贴题目/长回答）；② Enter 发送曾实现但与换行互斥（小程序无键盘事件无法区分 Shift+Enter），按用户决策舍弃 Enter 发送，**发送只走按钮**；③ 新增题目标签行"复制"按钮 + 消息文本 user-select（真机长按可选）。

**现状**：答辩页输入框是单行 `input`，文字很小，输入体验差。

**目标**：
1. 改成微信聊天式输入区：多行 `textarea` + 右侧发送按钮
2. Enter 键 → 换行（与微信一致），Shift+Enter 行为待定（默认与 Enter 相同即可）
3. 每行文字顶到输入区右端自动换行（textarea 天然支持）
4. 总字数限制：`maxlength` 限制总长（建议 200 字，数值可调）

**涉及文件**：
- `src/main/resources/static/Ai/pages/defense/defense.wxml`（输入区结构）
- `src/main/resources/static/Ai/pages/defense/defense.wxss`（输入区样式，按钮大小、圆角等）
- `src/main/resources/static/Ai/pages/defense/defense.js`（textarea bindinput / 发送逻辑，复用现有发送函数）

**验收**：
- 多行输入正常换行，文字到右端自动换行
- 超过总字数（200）无法继续输入，有提示
- 发送按钮点击后清空输入区、正常走答辩请求
- 视觉上与微信对话框接近（圆角、占位文案、按钮可点状态）

---

## 二、登录页键盘流转（P0，前端）✅ 已完成（2026-09-16，用户实测通过）

**现状**：账号/密码输入框默认键盘，Enter 行为未知，登录只能点按钮。

**目标**：
1. 账号输入框按 Enter → 光标自动跳到密码输入框
2. 密码输入框按 Enter → 触发登录
3. 登录失败 → 保持现有报错机制（错误提示、输入不清空或部分保留）

**涉及文件**：
- 登录页 wxml / js / wxss（`static/Ai/pages/login/` 附近，具体文件开工时确认）

**验收**：
- 账号 Enter → 密码框聚焦（软键盘弹起不关闭）
- 密码 Enter → 调用登录接口
- 错误账号/密码 → 现有错误提示机制不变

---

## 三、JVM + Ollama 资源加固（P1，后端/资源，可选做）

**背景**：机器仅 7G 内存，曾因内存耗尽崩溃（hs_err_pid*.log）；该改动**挂账中，用户确认暂不做**，开工前需再确认。

**目标**（不影响现有功能与体验为前提下做）：
1. JVM 堆上限：pom.xml 的 spring-boot-maven-plugin 加 `<jvmArguments>-Xmx512m</jvmArguments>`（或启动命令 `-Dspring-boot.run.jvmArguments=-Xmx512m`）
2. Ollama 并发限制：`setx OLLAMA_NUM_PARALLEL 1`（管理员命令行，重启 Ollama 生效）
3. 不换模型（`qwen2.5:3b-16k`，RTX 3050 4G 显存勿换大模型）

**验收**：答辩 10 轮全流程稳定，多场连测内存平稳（jstat/jconsole 观察堆不线性爬升）。

---

## 四、答辩记录详情页（P1，前端）

**现状**：记录页只显示列表（分数/时间/状态），点不开看每轮评分明细。

**目标**：
1. 记录列表点击 → 进入详情页
2. 展示：整场总分、每轮评分明细（轮次 / 表达 / 逻辑 / 专业 / 应变 / 创新 / 点评 / 题目）
3. 数据源：`defense_records` + `defense_score_record` + `defense_student_questions` / `defense_answers`

**涉及文件**：新增详情页（wxml/js/wxss/json）或复用现有页面跳转

**验收**：点击任意 completed 记录能看到逐轮明细；pending 记录可看已答部分或提示未完成。

---

## 五、语音输入完善（P1，前端，需先确认现状）

**现状**：库里有 `voice_responses` 表，前端似乎已有语音按钮（开工前先确认现状功能完整度）。

**目标**：
1. 盘点现有语音功能（按钮是否可用、识别结果是否正确进入回答）
2. 若已可用 → 补体验细节（识别中状态、结果回填输入框、取消/重录）
3. 若不可用 → 修复链路（录音 → 识别 → 回填）

**涉及文件**：答辩页 + 语音相关 js/wxml/wxss

**验收**：语音按钮可录音、识别结果回填输入区或直接发送，识别失败有提示。

---

## 六、备注

- 本文档为需求罗列，未实现任何改动；CHANGES.md 的改动记录在实现时再同步。
- 优先级：P0（输入框 + 登录流转）优先，P1 按需推进。
- 红线不变：不提交 `application.yml` / `*.log` / `hs_err_pid*`，不强推远程、不改历史。
