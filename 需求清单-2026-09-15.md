# AI-helper 需求清单（2026-09-17 重整版）

> 用途：开工前先看本清单，不凭记忆改代码。**本文档只记录需求，不包含实现。**
> 状态约定：`待开发` → `开发中` → `待实测` → `已完成`
>
> **本次重整（2026-09-17 晚）**：按「已完成 / 未完成（按优先级）/ 未完成汇总」三段式重组。
> 新增 **N42（评分判分过严，实测暴露）**、**N43（上传文件命名规范化）**、**N44（追问脱离课题，诊断衍生）**。
> 编号规则：`一~六` 为最初需求，`F*` 为功能改造归档，`N*` 为审计/诊断需求，`S*` 为评分逻辑改造。
>
> 红线不变：不提交 `application.yml` / `*.log` / `hs_err_pid*`，不强推远程、不改历史；硬件红线只能用 3B 模型。

---

# 第一部分 · 已完成的需求

## 1.1 完成清单

| 编号 | 需求 | 级别 | 类型 | 完成日 | 验证状态 | 备注 |
|---|---|---|---|---|---|---|
| 一 | 答辩输入框 → 微信式对话框（textarea + 发送按钮） | P0 | 前端 | 09-16 | 工具验证通过，真机待验 | `maxlength` 放宽至 2000；发送只走按钮 |
| 二 | 登录页键盘流转（账号 Enter → 密码，密码 Enter → 登录） | P0 | 前端 | 09-16 | 用户实测通过 | — |
| F1 | 弹层遮挡修复（教师端 / 学生端 / 答辩端） | P0 | 前端 | 09-17 | 工具验证通过 | 两端根因不同：样式冲突 vs 结构问题 |
| S1~S6 | 文字答辩评分逻辑改造 | P0 | 后端 | 09-17 | **待实测**（当日即被 N42 打回，见 1.3） | 双示例 / 两步判分 / 强制归零 / 问号 bug |
| F2 | 视频上传（真分片 + 格式大小双校验 + 预览 + 删除 + 续传） | P0 | 前后端 | 09-17 | 用户实测可上传 | 弃用阿里云 OSS，改本地磁盘 |
| F3 | 答辩报告上传（校验 + 真实进度 + 查看下载 + 删除） | P0 | 前后端 | 09-17 | 工具验证 | 后端原先**完全没有**格式校验 |
| F4 | 上传逻辑抽象为通用模块 | P0 | 前后端 | 09-17 | 已落地 | 前端 `utils/uploader.js`；后端 4 个 Service + `MediaKind` 枚举 |
| F5 | 附件上传提示条（废弃卡片内常驻绿字） | P1 | 前端 | 09-17 | 用户确认无旧效果残留 | 标题栏正下方文档流，成功绿 / 失败红，5 秒消失 |
| F6 | 上传相关越权修复（**部分**） | P0 | 后端 | 09-17 | 上传链路已验证 | 残留部分见 **N19** |
| F7 | 上传规则代码级默认值（yml 变可选覆盖） | P1 | 后端 | 09-17 | 已落地 | 换机器不会因白名单为空全部失败 |
| F8 | 存储目录自动选盘（三级降级，无 F 盘自动落 D 盘） | P1 | 后端 | 09-17 | 已落地 | `preferred-drives: F,D,E,C` |
| F9 | `application.yml.example` 配置模板 | P1 | 工程 | 09-17 | 已落地 | 同时闭环 **N13** |
| N23 | 忘记密码页请求体恒 `undefined`（功能不可用） | P0 | 前端 | 09-17 | — | `email: email` → `this.data.email` |
| N26 | `serverUrl` 开关不统一（7 处裸用 + 统一 12 处） | P1 | 前端 | 09-17 | — | 统一出口 `getBaseUrl()` |
| N28 | 401 统一处理（弹「登录已过期 → 去登录」） | P1 | 前端 | 09-17 | — | 不再白屏卡死 |
| N29 | 真机地址（写入真实 WLAN IP） | P0 | 前端 | 09-17 | — | V0 前置条件 |
| N39 | 小程序模板残留清理（13 个文件） | P3 | 前端 | 09-17 | — | `logs`/`index`/`reset`/`util.js` 等 |
| N13 | 配置模板化 | P3 | 工程 | 09-17 | — | 随 F9 一起完成 |

## 1.2 部分完成（残留部分已并入未完成清单）

| 编号 | 已完成部分 | 未覆盖部分 |
|---|---|---|
| F6 / N17 | `WebConfig` 注册 `AuthInterceptor`；保护范围 `/api/video/**`、`/api/report/**`、`/student/**`；token TTL 30 → 120 分钟 | `/teacher/**` 角色校验、`/editUserInfo` 只能改自己 → **N19** |
| N18 | 学生记录接口身份改取登录态 + 归属校验（拿他人 `defenseId` 被拒） | 其余按 id 直查的接口 → **N19 / 批次 B** |

## 1.3 评分逻辑改造（S1~S6）明细 + 当日实测反证

**已完成改动**（仅 `Controller/chatController.java`，7 处）：

| 编号 | 改动 | 目的 |
|---|---|---|
| S1 | prompt 双示例：示例1 切题 38 分、**示例2 跑题 0 分** | 铲掉唯一的高分锚点，给模型"跑题=0"的对照 |
| S2 | 判分两步法 + 分数档位（35~50 / 20~34 / 5~19 / 0） | 用规则表替代印象给分 |
| S3 | 追问阶段显式输出 `当前题:` | 旧实现追问阶段不给题目，模型只能闭眼打分 |
| S4 | 评分调用 `temperature(0.0)` | 同一回答的评分可复现（原 ±4 分） |
| S5 | 服务端 `isOffTopicMarked()` / `isOffTopicMarkedInResponse()` 命中 → 强制五维归零 + `forceZeroScoreLine()` 改写评分行 + `stripTopicMarker()` 剥离标记 | 把"给几分"从模型手里收回，改为确定性 |
| S6 | 修复 `extractQuestionFromLastAiMessage` 只认含问号行的 bug | 问号 bug 导致追问题目与答案行**均不落库**（N5 根因） |

**⚠️ 2026-09-17 当晚实测反证**：S5 把"切题判定"100% 托付给 3B 模型，模型一旦误判 [跑题]，服务端会**无条件归零**、零缓冲。实测 10 轮里 8 轮 0 分，其中至少 1 轮属明确误伤 → 已立 **N42**。

---

# 第二部分 · 未完成需求（按优先级排行）

级别定义：

| 级别 | 含义 | 判定标准 |
|---|---|---|
| **P0** | 答辩现场保命 / 阻塞 / 数据错乱 | 不做会现场翻车、或数据错乱、或已有改动未验证 |
| **P1** | 数据正确性与稳定性 | 不崩但会算错分 / 少落库 / 提前收尾 |
| **P2** | 体验与教学价值 | 不影响正确性，提升可用性与说服力 |
| **P3** | 工程与协作 | 长期健康度，随时可做 |

---

## P0

### N42、评分判分过严：[跑题] 误判导致有内容的回答被判 0 分 ★ 2026-09-17 实测暴露

**实测证据**（`defenseId=284`，`topicId=28`，学生 `12408060102`，10 轮；时间 2026-09-17 17:18~17:21，见 `logs/ai-helper.log`）：

| 轮 | 答题字数 | AI 判定 | 得分 |
|---|---|---|---|
| 1 | 309 | [跑题] | 0 |
| 2 | 528 | [跑题] | 0 |
| 3 | 8 | [跑题] | 0 |
| 4 | 8 | [跑题] | 0 |
| 5 | 88 | [跑题] | 0 |
| 6 | 70 | **[切题]** | 给分（NameNode） |
| 7 | 67 | **[切题]** | 给分（DataNode） |
| 8 | 77 | [跑题] | **0（误伤，见下）** |
| 9 | 5 | [跑题] | 0 |
| 10 | 3 | [跑题] | 0 |

收尾日志：`答辩收尾完成 - defenseId: 284, 总分: 6.9, 轮次数: 10`。

**误伤案例（第 8 轮，用户复现）**：

- 学生答：`RegionServer管理多个Region，负责数据读写、Region分裂与合并，与HDFS交互存储数据，并向Master汇报状态。` + `666这道题我不会`
- 模型原始返回：`点评:[跑题]回答内容与本题无关，未正面回应所问关于HBase中RegionServer的主要职责。\n评分:0/50|0|0|0|0|0`
- 紧随日志：`切题判定为跑题，强制五维归零 - defenseId: 284, 模型原总分: 0.0`

**三点结论**：

1. **0 分是模型自己给的**（"模型原总分: 0.0"），服务端的强制归零只是把 0 又写成 0，**不是元凶**；
2. **模型的跑题理由是编的** —— 答案通篇就是 RegionServer 的职责（管理 Region、读写、分裂合并、与 HDFS 交互、向 Master 汇报），它却说"未正面回应"。是提示词"点评必须以 [切题]/[跑题] 开头"逼出来的自洽；
3. **唯一变量是末尾那句"666这道题我不会"** —— 第 6/7 轮 70/67 字同类回答均判 [切题] 给分，第 8 轮 77 字、内容同样是职责描述，只多了这句自嘲。

**根因（按权重）**：

| 权重 | 位置 | 问题 |
|---|---|---|
| 主因 | `chatController.java:285` | 「特别注意」段把"回答里出现了'不会'类短语"**直接等同于**"整段回答无实质内容"，**没有长度/占比门槛**；同段还列举「报数玩笑（如"我是250"）」「666」「纯数字」等词，3B 模型读作"命中关键词即触发" |
| 放大器 | `chatController.java:472-488` | 服务端把"切题/跑题"完全托付给 3B 模型，命中 [跑题] 就**无条件五维归零**，模型误判零缓冲 |
| 同方向钩子 | `chatController.java:284`、`:281` | 判分第二步「答非所问、含糊其辞、"不知道" = 0」；示例2 专门示范 `[跑题] → 0/50\|0\|0\|0\|0\|0` |
| 诱因 | `chatController.java:1180-1236` | 追问阶段题目脱离课题，人为抬高切题判定难度 → 见 **N44** |

**对照事实**：服务端 `isGiveUpAnswer` 本来有长度闸门（`GIVE_UP_MAX_LENGTH = 15`，`:1013-1014`），77 字长回答根本不会走固定零分流程 —— **缺的正是 prompt 里同样的闸门**。

**目标（需拍板，可组合）**：

- **方案 A（改 prompt，最小改动）**：给规则加门槛 —— 明确"仅当**整段回答**就是这类短语/敷衍内容时才判 0；回答若有实质内容，即使夹带一句自嘲/口头语，也按内容正常评分"。把"666""我是250"等例子从通用规则中剥离，避免被当关键词匹配。
- **方案 B（服务端兜底，建议与 A 同做）**：命中 [跑题] 时先做一次复核 —— 回答长度 ≥ 30 字且含题目关键词 → 不直接归零，降档给分或走"保守给分"分支。
- **方案 C（成本最低，可顺手做）**：把 `isJunkAnswer` 的 15 字闸门理念同步进 prompt（回答 > 50 字时，不得仅凭出现"不知道/不会"判 0）。

**涉及文件**：`Controller/chatController.java`（`:281`、`:284`、`:285`、`:472-488`、`isOffTopicMarked` / `isOffTopicMarkedInResponse` / `forceZeroScoreLine` / `stripTopicMarker`）

**验收**：

- 复现用例（RegionServer + `666这道题我不会`）→ **不得判 0**，应落正常档位分；
- 纯 `我不会`（≤15 字）→ 仍走固定 0 分流程；
- 纯 `666` / `我是250` / `？？` → 仍 0 分；
- 一段与题目完全无关的 500 字文字 → 仍 0 分（S5 的初衷不能回退）。

---

### N19、教师端接口无角色校验 → 学生可删改课题、改他人资料 ★ 高危

**证据**：

- `Controller/teacher/DefenseController.java:22-47`（`/teacher/addDefense`、`/teacher/editDefense`、`/teacher/deleteDefenseTopics`）无任何角色判断
- `Controller/teacher/DefenseRecordsController.java:35-65`（查全站记录/详情）无角色判断
- `Controller/login/editUserInfoController.java:18-21` + `Service/Impl/EditUserInfoServiceImpl.java:16-20`：`/editUserInfo` 直接用请求体 `id` 更新 → **任意改他人姓名/学号/邮箱**

**目标**：引入角色校验（Redis 或 JWT claim）；教师接口要求 `role=teacher`；`/editUserInfo` 的 id 必须等于登录用户。
**验收**：学生账号调用教师接口被拒；用户只能改自己的资料。

---

### N1、中途退出后重进答辩 → 记录错乱 ★ 真实 bug（方案已定 **A：续答**）

**代码证据**：

- `chatController` 的 `/api/chat/clear` → `DefenseRecordsServiceImpl.startNewDefenseRecord()`（`DefenseRecordsServiceImpl.java:274`）
- `deleteEmptyShellRecords` 只删除「一题未答」的记录（`resources/Mapper/DefenseRecordsMapper.xml:232-238`）
- `startNewDefenseRecord` 删完**无条件新建**一条 pending 记录（`:288`）
- `getDefenseIdByUserAndTopic` 只取 pending 中 `defense_id DESC` 最新一条（`:208-213`）

**问题链路**：学生答 3 题 → 退出 → 重进答辩页 → `clear` 清空 Redis 记忆 + 旧记录非空壳（不删）+ **又建一条新空壳** → `getOrCreate` 取到新空壳（0 题）→ 学生从第 1 题重来，而原来答了 3 题的记录**永远卡在 pending**，在记录列表显示"未完成"，成为孤儿记录。

**目标（已拍板方案 A）**：重进时若存在「有作答的 pending 记录」→ **不新建记录**，从 `defense_score_record` + `defense_answers` 回灌 Redis 记忆继续答。

**涉及文件**：`Service/Impl/DefenseRecordsServiceImpl.java`、`Controller/chatController.java`、`resources/Mapper/DefenseRecordsMapper.xml`、`static/Ai/pages/defense/defense.js`

**验收**：答 3 题退出→重进，不产生孤儿记录、不出现两条并发 pending、轮次与答辩记录一一对应。

---

### N3、teacher 页 fixed 悬浮层文档流改造

**现状**：student / defense 两页已完成「100vh flex 三段式」根治渲染残影（CHANGES.md 9-16 节）；`teacher.wxss` 仍有 4 处 `position:fixed`（`:14` / `:697` / `:726` / `:893`），同款隐患未处理。

**目标**：套用已验证方案 —— `.page` 固定 100vh + `overflow:hidden`，header / tab-bar 回归文档流，内容区 `flex:1 + min-height:0`。

**涉及文件**：`static/Ai/pages/teacher/teacher.wxss`、`teacher.wxml`
**验收**：教师页滚动、切页无叠层残影；真机走查通过。

---

### N2、答辩现场环境一键自检

**现状**：`diagnose.ps1` 只查 Ollama 状态 / 显存 / 推理速度；MySQL、Redis、8080 端口、模型是否已加载均不检查。

**目标**：扩展为"答辩前 30 秒自检"，逐项检查：MySQL 连通 + `defense_score_record` 表存在 / Redis 连通（密码 123456）/ Ollama 在跑且 `qwen2.5:3b-16k` 已 pull / 8080 端口占用 / 可用物理内存。

**涉及文件**：`diagnose.ps1`（或新增 `preflight.ps1`）
**验收**：一条命令输出逐项 ✅/❌ 与修复提示，避免现场"以为都开着"翻车。

---

### V0、前端真机验证

**现状**：9-16 / 9-17 两批前端改动（输入框、弹层、上传、提示条）均只在微信开发者工具验证过。

**目标**：真机走查 —— 输入框多行输入与发送、页面切换无残影、上传视频/报告全流程、提示条位置与消失、10 轮答辩全流程。

**前置**：`utils/config.js` 的 `useRemoteServer` 改为 `true`，手机与电脑同一 WiFi，放行 8080 入站。
**验收**：真机全流程无阻断，问题记录回本清单。

---

## P1

### N43、上传文件命名规范化（UUID → 可读文件名）

**现状**：磁盘文件名是 32 位无横线 UUID。F 盘实测：

```
F:\ai wordplace\reports\83e2c7ee373248148b72cc35a94fbe9e.docx
F:\ai wordplace\reports\f995a82a76b14e8f9ee1ba65fd1e624d.xlsx
F:\ai wordplace\videos\5cd030825a8b43038aed1716f7acb2fc.mp4
```

用户上传的 `陈泓伍-简历.docx` 落盘后完全不可辨识；报告与视频都一样。

**代码位置**：

- `Service/Impl/LocalFileStorageServiceImpl.java:279-289` → `generateFileName()` = `IdUtil.simpleUUID() + suffix`（唯一入口）
- 调用点：`Service/Impl/ChunkUploadServiceImpl.java:83`（视频，`init` 时定型并存入 Redis 会话）、`Service/Impl/MediaFileServiceImpl.java:52`（报告，直传）

**目标**：磁盘文件名保留可读信息，形如 `{业务类型}_{学号}_{原始文件名}_{短随机}.{ext}`。

候选格式（**需拍板**）：

| 方案 | 示例 | 说明 |
|---|---|---|
| **1（推荐）** | `答辩报告_12408060102_陈泓伍-简历_a1b2c3.docx` | 信息最全：归属 + 原名 + 唯一性 |
| 2 | `报告_12408060102_20260917165650.docx` | 更短，不保留原名，靠时间戳区分 |
| 3 | `陈泓伍-简历_a1b2c3.docx` | 最贴近用户举例，**不含学号**，归属需查库 |

**必须处理的技术约束**：

1. **文件名 sanitize**：剔除 `/\ : * ? " < > |`、控制字符、`..`、首尾空格与点（Windows 非法字符 + 目录穿越）；原始名长度上限（建议截断到 40 字）；原始名为空时退化为纯 UUID。
2. **URL 编码（最大风险点）**：`toPublicUrl()`（`LocalFileStorageServiceImpl.java:199-213`）是**字符串直接拼接**。文件名一旦含中文/空格，进入 URL 必须 URL 编码（后端 `URLEncoder` + `encodeURIComponent` 语义，或前端 `encodeURI`），否则视频 Range 请求、`wx.previewMedia`、`openDocument` 会 404。**此项必须与命名改造一起验证。**
3. **唯一性**：同一学生重复上传同名文件会互相覆盖 → 必须带时间戳或短随机串。
4. **接口变更**：`FileStorageService.generateFileName(String)`（`:66`）需扩展为调用方传入「学号 + kind + 原始名」（改签名或加重载）。
5. **学号来源**：视频 `init` 已有 `loginUserNumber`（`ChunkUploadServiceImpl:53`）、报告已有 `loginUserNumber`（`MediaFileServiceImpl:34`）；**姓名需额外查 `users` 表** —— 若命名方案含姓名则要加查询。
6. **历史文件**：存量 UUID 文件**不重命名**（URL 已落库，改名即失效），只对新上传生效；如需统一，另做迁移脚本（DB + 磁盘同步改）。

**涉及文件**：`Service/FileStorageService.java`、`Service/Impl/LocalFileStorageServiceImpl.java`、`Service/Impl/ChunkUploadServiceImpl.java`、`Service/Impl/MediaFileServiceImpl.java`、`util/UploadUtils.java`、`Config/WebConfig.java`

**验收**：

- 上传 `陈泓伍-简历.docx` → 磁盘出现可读文件名，URL 可正常访问 / 预览 / 下载 / 删除 / 替换；
- 连续上传两个同名文件 → 两份都在磁盘，不互相覆盖；
- 文件名含中文、空格、括号、超长 → 全流程正常；
- 旧 UUID 文件仍可访问（历史答辩记录不失效）。

---

### 新-1、样式类重复定义去重

**现状**：全项目扫描发现 `teacher.wxss` 13 个类、`student.wxss` 2 个类存在**两套定义**，后写静默覆盖（F1 的教师端弹层遮挡就是这个坑）。已扫描未处理。

**目标**：逐个合并去重，保留生效的那套；顺手去掉弹层内嵌套的 `scroll-view`。
**验收**：去重后教师端 / 学生端视觉与当前一致，无新的遮挡或错位。

---

### 新-2、`upsertDefenseRecord` 依赖已失效的唯一索引

**现状**：`(user_id, topic_id)` 唯一索引疑似已被改为普通索引（CHANGES.md 9-10 记录）→ `upsertVideoUrl` / `upsertReportUrl` / `upsertDefenseRecord` 会**退化成纯 INSERT**（每次上传/答复新建一条记录），这正是当时"172 条重复空壳记录"的成因之一。上传链路已改为按 `defense_id` 显式 UPDATE 绕开，**但 `upsertDefenseRecord` 尚未处理**。

**前置核验**：`SHOW INDEX FROM defense_records;` 确认唯一索引是否还在。
**目标**：与 N1 一起改（同区域）。

---

### N20 / N21 / N22、事务与并发（同一链路，建议合并做）

**N20 核心写流程无 `@Transactional`，且已有事务因吞异常而失效**

- 全项目仅 `Service/Impl/DefenseTopicsServiceImpl.java:25`、`:113` 有 `@Transactional`；`DefenseRecordsServiceImpl`、`ScorePersistenceServiceImpl`、`RegisterServiceImpl`、`PasswordResetServiceImpl` 的多表写入均无
- `DefenseRecordsServiceImpl.java:183-186 / 228-230 / 267-270 / 292-294 / 310-312` 全部 `catch(Exception){log...}` 静默吞掉 → 半截数据 + 上层无感知
- `DefenseTopicsServiceImpl` 的 `@Transactional` 方法内部 `catch` 后 `return`，异常不上抛 → **事务不回滚**（"有注解也失效"）

**目标**：写流程补 `@Transactional(rollbackFor = Exception.class)`；吞异常处改为记录上下文并上抛或补偿。

**N21 `@Async` 评分落库静默失败 + 轮次计数竞态**

- `Service/Impl/ScorePersistenceServiceImpl.java:34-46` `saveRoundScoreAsync` 为 `@Async void`，内部 catch 只 log → 落库失败完全静默
- `chatController.java:143-155` 先 `getOrCreateDefenseRecord` 再 `countByDefenseId` 非原子，而轮次/收尾判断依赖"评分行已落库" → 异步延迟导致少算、并发重试可致重复行或提前/延后收尾

**说明**：这是"前端重试重复评分行 / 提前收尾"的更深层根因（与 N4 同源）。
**目标**：轮次计数改为数据库侧原子计数（或落库改同步）；异步失败可见（返回状态/告警）。

**N22 ChatMemory 的 Redis 锁形同虚设**

- `Config/ChatConfiguration.java:96-102` 取锁后，失败分支（`:146-171`）会走**无锁强制写** → 并发下重复写入会话消息

**目标**：去掉"无锁强制写"兜底，改为等待/重试或返回提示。

**验收（三条共）**：模拟落库失败不出现半截数据；并发重发同一轮请求，轮次单调递增不跳号、不重复落行；并发下会话消息不重复。

---

### N4、评分行写入幂等

**现状**：前端重试会重发同一轮请求，后端异步落库无幂等键 → 同轮落两行 → 权威计数 +1 → **提前一轮收尾**。
**目标**：按 `(defense_id, round_num)` 唯一化 —— 加唯一索引，或落库前先判定是否已存在。
**涉及文件**：`docs/ddl_defense_score_record.sql`、`Service/Impl/ScorePersistenceServiceImpl.java`、`resources/Mapper/DefenseScoreRecordMapper.xml`
**验收**：同一轮请求重发 3 次，`defense_score_record` 仅 1 行，轮次不跳号。

---

### N5、追问阶段放弃作答答案行落库遗漏

**现状**：追问阶段放弃作答时，答案行可能因 Redis 历史被裁剪而跳过落库（评分行仍在），日志有 WARN。
**目标**：放弃作答路径不再依赖 Redis 历史定位当前题，改用 `defense_student_questions` / 当前轮次落库。
**涉及文件**：`Controller/chatController.java`（`handleGiveUpAnswer`）、`Service/Impl/DefenseRecordsServiceImpl.java`
**验收**：追问阶段回答"不会"，`defense_answers` 与 `defense_score_record` 行数一致。

> 注：S6 已修掉"只认问号行"这个诱因，本项仍需兜底。

---

### N6、总分 / 五维口径统一

**现状**：模型偶发"总分 ≠ 五维之和"，落库与展示已按五维和纠正，但 **AI 气泡原文数字与页面数字对不上**。
**目标**：后端在写回响应前就把评分行规范化为五维和（或前端展示统一取后端校验后的值），消灭"同屏两个数"。
**涉及文件**：`Controller/chatController.java`、`Service/Impl/ScorePersistenceServiceImpl.java`、`defense.js parseSegmentFormat`
**验收**：任意一轮，气泡总分 / 评分区块 / DB `defense_score_record` 三处数字一致。

---

### N24 / N27、前端登录态与超时治理

**N24 退出登录清理的 storage key 与登录写入的不一致**

- 登录写入 `token`/`userName`/`userRole`/`userInfo`（`pages/login/login.js:144/147/150/153`）；退出却删 `token`/`user`/`role`（`pages/student/student.js:287-289`、`:1596-1598`）→ `userInfo` 等**从未被清除**，切账号后仍显示上一个用户信息
- **目标**：统一 key（建议抽 `utils/auth.js` 提供 `clearLogin()`）

**N27 全局 `networkTimeout.request = 10000` 与答辩 180s 冲突**

- `static/Ai/project.config.json:51-52` `"request": 10000`；`defense.js:138` 单请求可覆盖为 180000，但 `teacher.js`/`student.js` 所有请求**未设 timeout** → 弱网/慢查询提前失败
- **目标**：全局超时提到合理值（如 60s），答辩类接口显式 180s

---

### N7、AI 评分稳定性

**实测证据**：同一句"我不会这道题"三次得分 25 / 0 / 20，口径不一。
**现状**：S4 已落地 `temperature=0`，但模型判"切题/跑题"本身仍不稳定（见 N42）。
**目标（与 N42 合并考虑）**：① 保持 `temperature=0`；② 判分规则表化写进 prompt；③（可选）同一回答采样 3 次取中位数。
**验收**：同一回答连续 5 次评分方差明显收敛；同一份"切题"回答不再出现 0 / 满分两极跳变。

---

### 三（现有）、JVM + Ollama 资源加固 —— 挂账中

**背景**：机器仅 7G 内存，曾因内存耗尽崩溃（`hs_err_pid*.log`）；**用户确认暂不做**，答辩前需再确认。

**目标**：

1. JVM 堆上限：`pom.xml` 的 spring-boot-maven-plugin 加 `<jvmArguments>-Xmx512m</jvmArguments>`
2. Ollama 并发限制：`setx OLLAMA_NUM_PARALLEL 1`（管理员命令行，重启 Ollama 生效）
3. 不换模型（`qwen2.5:3b-16k`，RTX 3050 4G 显存勿换大模型）

**验收**：答辩 10 轮全流程稳定，多场连测内存平稳（`jstat`/`jconsole` 观察堆不线性爬升）。

---

## P2

### N44、追问阶段题目脱离课题 ★ 诊断衍生（N42 的诱因）

**证据**：`defenseId=284` 的后 5 轮追问问出「HBase 中 RegionServer 的主要职责」「NameNode 的作用」「DataNode 的主要职责」，而课题是「城市污染数据可视化」。`generateFollowUpQuestion()`（`chatController.java:1180-1236`）只把课题名塞进 prompt，**没有"必须围绕课题"的硬约束** → 题目飘到通用大数据知识，进一步抬高"切题判定"难度（学生按题意作答，评委却拿"本课题的可视化"去对）。

**目标**：① prompt 增加硬约束（追问必须围绕课题名 / 课题描述 / 学生已答内容）；② 生成后做一次课题相关性校验（复用已有 Dice 相似度），不达标则重试或改用兜底题。
**涉及文件**：`Controller/chatController.java:1180-1236`
**验收**：一场 10 轮答辩里追问不再出现与课题无关的通用常识题。

---

### N25、`student.js` 有两个同名 `logout()`，后者覆盖前者

**证据**：`pages/student/student.js:277-299` 与 `:1586-1607` 同名 → JS 字面量后者覆盖前者，第一个是死代码（本项目已发生过同类"改了不生效"事故）。
**目标**：删除死代码，只保留一处。

---

### N8 / 四（现有）、答辩记录详情页（+ 五维雷达图 / 标准答案对照）

**现状**：记录页只显示列表（分数/时间/状态），点不开看每轮评分明细。

**目标**：

1. 记录列表点击 → 进入详情页；展示整场总分 + 每轮评分明细（轮次/表达/逻辑/专业/应变/创新/点评/题目）
2. **五维雷达图**（小程序 canvas，无需新依赖）
3. **教师视角叠加标准答案对照**（`defense_questions.standard_answer`），学生视角不给（防泄题）

**数据源**：`defense_records` + `defense_score_record` + `defense_answers` + `defense_questions`
**涉及文件**：新增详情页（wxml/js/wxss/json）或复用现有页面跳转
**验收**：`completed` 记录可见逐轮明细 + 雷达图；`pending` 记录提示未完成。

---

### N12 / 五（现有）、语音输入完善

**现状**：库里有 `voice_responses` 表，前端疑似有语音按钮，完整度待盘点。
**目标**：① 盘点现状（按钮可用性 / 识别结果是否进入回答）；② 可用则补体验（识别中状态、结果回填、取消重录）；③ 不可用则修链路（录音 → 识别 → 回填）。
**涉及文件**：`pages/defense/` 语音相关 js/wxml/wxss
**验收**：语音按钮可录音、识别结果回填输入区或直接发送，识别失败有提示。

---

### N9、教师端成绩导出与统计

**现状**：教师端只能逐条查记录，无汇总视图。
**目标**：① 按课题导出成绩表（CSV/Excel：学生、总分、五维、用时）；② 课题维度统计（参与人数、平均分、五维雷达，看全班薄弱环节）；③（可选）学生端生成"答辩成绩单"长图保存相册（`canvasToTempFilePath`）。
**涉及文件**：`Controller/teacher/`、`Service/`、`pages/teacher/`
**验收**：教师一键导出班级成绩文件；统计页五项均值与明细可核对。

---

### N10、教师端题库批量导入

**现状**：`/teacher/addDefense` 只能一题一题加；现有题库靠人工 SQL 补到每课题 5 道。
**目标**：Excel/CSV 模板批量导入（题面 + 标准答案 + 类型），导入前预览 + 重复题拦截（复用已有 Dice 相似度 / `cleanNextQuestion` 逻辑）。
**涉及文件**：`Controller/teacher/DefenseController.java`、`Service/DefenseTopicsService.java`、`pages/teacher/`
**验收**：20 道题一次导入成功；重复题被识别提示。

---

### N11、防作弊：粘贴题目检测

**现状**：把题目文本复制当回答，模型判定不一致（有的 0 分"无实质内容"，有的 27/50"思路清晰"）。
**目标**：提交时做"回答 vs 当前题目"相似度检测（复用后端归一化 + Dice 相似度工具），高度相似 → 直接走固定 0 分流程，**不调用评分模型**（与 `JUNK_ANSWER_PHRASES` 同一条路径）。
**涉及文件**：`Controller/chatController.java`、`defense.js`
**验收**：复制题目原文提交 → 0 分 + 提示"请勿直接复制题目"。

---

### N30、多处 NPE / 越界 / 逻辑优先级

| 位置 | 问题 |
|---|---|
| `Controller/student/VideoUploadController.java:28` | `fileName.substring(fileName.lastIndexOf("."))` 无扩展名时 `lastIndexOf` 返回 -1 → `StringIndexOutOfBoundsException` |
| `Service/Impl/RegisterServiceImpl.java:74` | `a == null \|\| b.trim().isEmpty() && "teacher".equals(role)` —— `&&` 优先级高于 `\|\|`，且学生分支 `:77` 对可能为 null 的 `userNumber` 调 `.trim()` → NPE |
| `Service/Impl/RegisterServiceImpl.java:91` | `dbUser.getRole().equals(...)`，库中 role 为 null 则 NPE |
| `Controller/chatController.java:189/193/204` | `getOrDefault("prompt","")` 遇"键存在值为 null"仍返 null → `.toString()` NPE；`Integer.parseInt` 未捕获 `NumberFormatException` |

---

### N31、错误信息透传前端 + 大量 `System.out` / `printStackTrace`

**证据**：`Service/Impl/DefenseTopicsServiceImpl.java` 约 15 处 `System.out`（`:28-33,53-54,58,71,74,80-82,151`）、`:80`/`:151` 用 `e.printStackTrace()`；且 `addDefense`/`editDefense` 把 `e.getMessage()`（含 SQL 细节）返回给前端。
**目标**：统一 `@Slf4j` 日志；对外统一模糊错误文案。

---

### N32、`GlobalExceptionHandler` 覆盖过窄

**证据**：`Config/GlobalExceptionHandler.java:17-28` 只处理 `ClientAbortException`/`IOException`，且处理方法返回 `void`；其他异常直接 500，前端拿不到 `Result` 结构。
**目标**：补 `Exception` 等统一处理，返回统一 `Result`。

---

### N33、视频处理状态存进程内 Map，且 `processingId` 调用方拿不到

**证据**：`Service/Impl/VideoProcessingServiceImpl.java:19` 用进程内 `ConcurrentHashMap`；`:28-84` 的 `@Async` 方法内**自己生成** `processingId`，调用方无从获取 → `Controller/student/VideoUploadController.java:90-94` 的 `/api/video/processing-status` 实际永远查 `not_found`；进程重启即丢。
**目标**：状态落 Redis；`processingId` 在提交时生成并返回前端。

---

### N34、上传孤儿文件 + 流未关闭（OSS 时代遗留）

**证据**：`Controller/student/ReportUploadController.java:57-85` 用 `file.getInputStream()` 未 try-with-resources；`:68-71` 若 `internalUserId == null` 直接 return → 已上传对象成为孤儿。另 `util/OssRedisUploadUtil.java:114-133` 在合并成功后、`redisTemplate.delete` 前抛异常会残留 Redis 分片记录。

> 注：本地磁盘改造（F2~F4）已修掉主链路，本项需按新实现复核是否仍有残留。

---

### N35、硬编码与配置重复

| 位置 | 问题 |
|---|---|
| `Service/Impl/PasswordResetServiceImpl.java:52` | 重置链接硬编码 `http://localhost:8080/reset-password` |
| `Service/Impl/PasswordResetServiceImpl.java:54` | 发件人硬编码 `1685975918@qq.com` |
| `Controller/chatController.java:424`、`:1161` | 模型名硬编码 `"qwen2.5:3b-16k"`（与 `application.yml:11` 重复，易不一致） |
| `Controller/login/ResetController.java:21-33` | 整段 HTML（含 CSS、表单 action）硬编码进 Java |
| `application.yml:23/30` | MySQL、Redis 密码均 `123456` |

---

### N38、`EditDefenseDto` 日期解析失败静默返回 null，导致写入 `"null"` 字符串

**证据**：`pojo/dto/EditDefenseDto.java:34-43` 解析失败返回 null 不报错；`Service/Impl/DefenseTopicsServiceImpl.java:42-46` 只在 `defenseTime == null` 时校验；`resources/Mapper/DefenseTopicsMapper.xml:25` 把 `String` 直接写入 → 非法日期会存成字符串 `"null"`。

---

## P3

### N36、全表查询无分页 / 循环内查询

**证据**：`resources/Mapper/DefenseRecordsMapper.xml:96-107` `getDefenseTopic` 全表无分页（学生每次进页拉全量课题）；`Controller/chatController.java:1199`（`collectAskedQuestions`）、`:1428`（`findExistingStudentQuestion`）每轮全量查同表后内存遍历。另 `DefenseTopicsMapper.xml:32` 的 offset 由 `(pageNum-1)*pageSize` 计算，极大 pageNum 可致 LIMIT 溢出/全表扫描。

---

### N37、分数用 `double` 累加存在精度误差

**证据**：`Controller/chatController.java:1342-1347`、`:1371-1380`、`:1392` 与 `Service/Impl/ScorePersistenceServiceImpl.java:137-162` 用 `double` 累加五维后再 `Math.round(x*10)/10.0` 转 `BigDecimal`，中间有 0.1 级浮点误差。落库字段本身是 `BigDecimal`。
**目标**：全链路改用 `BigDecimal`。

---

### N40、`project.config.json` 双份不同步

**证据**：根 `project.config.json:22` 与 `static/Ai/project.config.json:39` **appid 相同**，但 static 版还含 `libVersion`/`networkTimeout`/`requiredBackgroundModes`，根版缺失；根 `project.private.config.json:3` `"projectname":"ai"` 且**没有 `miniprogramRoot`**，若开发者工具打开根目录则找不到 `app.json`。
**目标**：确认并只保留一份，避免"改了不生效"。

---

### N41、全站 HTTP 明文 + 关闭域名校验

**证据**：`static/Ai/utils/config.js:8/10` 均为 `http://`；`static/Ai/project.private.config.json:6` `"urlCheck": false`。
**说明**：校内工具/真机调试可接受，但需明确知悉：答辩数据（含报告/视频地址）走明文传输。

---

### N14、核心纯函数最小回归测试

**目标**：给最容易手抖回归的纯函数加 JUnit：`parseScoresFromResponse()`、`isJunkAnswer()`、`isOffTopicMarked()`、`cleanNextQuestion()`。
**理由**：9-15 一天内改了 4 轮敷衍判定，每次只靠"实测一场"验证，成本高且不可复现；N42 改完更需要回归网。

---

### N15、答辩场次审计日志

**目标**：每场答辩落一条审计记录：开局（defenseId/user/topic）、每轮判定路径（模型评分 / 放弃作答 / 敷衍 / 兜底收尾）、收尾原因（模型总结 / 占位总结 / 硬上限）。答辩现场出问题可自查，不必翻全量日志。
**理由**：本次 N42 的诊断全靠翻 1.75MB 全量日志，成本高。

---

### N16、Git 协作阻塞处理

**现状**：远程 `github.com/kunkun-text/ai_helper` 是别人的仓库，本机账号 `chew303-cmd` 未被加为协作者，推送 403。
**目标**：改为 fork 到自己账号后推送，或请仓库所有者加协作者。**这是当前唯一的现实协作阻塞项。**

---

# 第三部分 · 未完成需求汇总表

> 排名即建议开工顺序（同级别内按"收益 / 成本比"排）。工作量：S ≈ 半小时内 / M ≈ 半天 / L ≈ 一天以上。

| 排名 | 编号 | 需求 | 级别 | 类型 | 前置依赖 | 工作量 |
|---|---|---|---|---|---|---|
| 1 | **N42** | 评分判分过严（[跑题] 误判归零） | **P0** | 后端 | 需拍板方案 A/B/C | S~M |
| 2 | **N1** | 中途退出后重进 → 记录错乱（**已定方案 A：续答**） | **P0** | 后端 | 核验索引（新-2） | M |
| 3 | **N19** | 教师端角色校验 + `/editUserInfo` 只能改自己 | **P0** | 后端 | 批次 B 剩余 | M |
| 4 | **N3** | teacher 页 header / tab-bar 文档流改造 | **P0** | 前端 | 9-16 方案已就绪 | M |
| 5 | **N2** | 答辩现场环境一键自检 | **P0** | 脚本 | — | S |
| 6 | **V0** | 前端真机验证 | **P0** | 验证 | N29 已就绪，改 `useRemoteServer=true` | S |
| 7 | **N43** | 上传文件命名规范化（UUID → 可读名） | **P1** | 全栈 | 需拍板命名方案 + URL 编码验证 | M |
| 8 | **N20/N21/N22** | 事务缺失 / `@Async` 静默失败 / ChatMemory 锁失效 | **P1** | 后端 | 同链路，建议合并一次改 | M |
| 9 | **N4/N5/N6** | 评分行幂等 / 追问放弃落库 / 总分口径统一 | **P1** | 后端 | 与 8 同链路，建议合并 | M |
| 10 | **新-1** | 样式类重复定义去重（`teacher.wxss` 13 + `student.wxss` 2） | **P1** | 前端 | 已扫描未处理 | S |
| 11 | **新-2** | `upsertDefenseRecord` 依赖已失效的唯一索引 | **P1** | 后端 | 需先 `SHOW INDEX` 核验，与 N1 同改 | S |
| 12 | **N7** | AI 评分稳定性（判分规则表化 / 多次采样） | **P1** | 后端 | 与 N42 合并考虑 | S |
| 13 | **N24/N27** | 登录态 storage key 不一致 / 全局 10s 超时 | **P1** | 前端 | 建议抽 `utils/auth.js` | S |
| 14 | **三（现有）** | JVM + Ollama 资源加固 | **P1** | 资源 | **挂账中，答辩前建议做** | S |
| 15 | **N44** | 追问阶段题目脱离课题 | **P2** | 后端 | N42 的诱因 | S |
| 16 | **N8/四** | 答辩记录详情页（+ 雷达图 / 标准答案对照） | **P2** | 前后端 | 为 N9 打数据基础 | L |
| 17 | **N9** | 教师端成绩导出与统计 | **P2** | 前后端 | 依赖 16 的数据组织 | L |
| 18 | **N10** | 教师端题库批量导入 | **P2** | 前后端 | 复用 Dice 相似度 | M |
| 19 | **N11** | 防作弊：粘贴题目检测 | **P2** | 前后端 | 复用 Dice 相似度 | S |
| 20 | **N12/五** | 语音输入完善 | **P2** | 前端 | 需先盘点现状 | M |
| 21 | **N25** | `student.js` 重复 `logout()` 死代码 | **P2** | 前端 | — | S |
| 22 | **N30** | NPE / 越界 / 逻辑优先级 | **P2** | 后端 | — | S |
| 23 | **N31** | 错误信息透传 + `System.out`/`printStackTrace` | **P2** | 后端 | — | S |
| 24 | **N32** | `GlobalExceptionHandler` 覆盖过窄 | **P2** | 后端 | — | S |
| 25 | **N33** | 视频处理状态落 Redis + `processingId` 可获取 | **P2** | 后端 | — | M |
| 26 | **N34** | 上传孤儿文件 + 流未关闭（按新实现复核） | **P2** | 后端 | — | S |
| 27 | **N35** | 硬编码（重置链接 / 发件人 / 模型名 / HTML / 弱密码） | **P2** | 后端 | — | S |
| 28 | **N38** | 日期解析失败写入 `"null"` 字符串 | **P2** | 后端 | — | S |
| 29 | **N36** | 全表查询无分页 / 循环内查询 | **P3** | 后端 | — | M |
| 30 | **N37** | 分数 `double` 精度 | **P3** | 后端 | — | M |
| 31 | **N40** | `project.config.json` 双份不同步 | **P3** | 工程 | 需先确认打开路径 | S |
| 32 | **N41** | HTTP 明文 + `urlCheck` 关闭 | **P3** | 工程 | 知悉即可 | S |
| 33 | **N14** | 核心纯函数最小回归测试 | **P3** | 工程 | N42 改完收益最高 | M |
| 34 | **N15** | 答辩场次审计日志 | **P3** | 工程 | — | M |
| 35 | **N16** | Git 协作阻塞（fork / 加协作者） | **P3** | 协作 | — | S |

## 建议批次

| 批次 | 内容 | 说明 |
|---|---|---|
| **批次 A'（立即，评分可信度）** | **N42 → N44** | 答辩刚刚实测翻车点；N42 是"用户能直接感知的错"，优先级等同 P0 |
| **批次 B（鉴权收尾）** | N19 → N28（已完成） | 一次性闭环角色校验 |
| **批次 C（数据一致性）** | 新-2 → N1 → N20/N21/N22 → N4/N5/N6 | 全在答辩落库链路，合并改一次省多轮重启实测 |
| **批次 D（上传规范化）** | N43 → N34 | 命名 + URL 编码 + 孤儿复核一起验 |
| **批次 E（前端治理）** | 新-1 → N24/N27 → N25 | 样式去重 + 登录态 + 死代码 |
| **批次 F（答辩保命）** | N3 → N2 → V0 → 三（资源加固） | 现场前最后一轮加固与真机走查 |
| **批次 G（体验 / 教学）** | N8 → N12 → N9 → N10 → N11 | — |
| **批次 H（工程收尾）** | N14 → N15 → N36 → N37 → N30~N35/N38 → N40/N41 → N16 | — |

---

# 第四部分 · 开工前需要你拍板

1. **N42 怎么改**？选 A（改 prompt 加门槛）/ B（服务端复核兜底）/ C（长度闸门同步进 prompt）—— 可组合，建议 **A + C**（成本最低）或 **A + B + C**（最稳）。
2. **N43 选哪个命名方案**？方案 1（`答辩报告_学号_原名_短随机`，推荐）/ 2（`报告_学号_时间戳`）/ 3（`原名_短随机`）。
   - 若方案含**姓名** → 需要额外查 `users` 表，改动多一处。
   - 确认**存量 UUID 文件不重命名**（只对新上传生效）是否可以。
3. **新-2 的索引核验**：是否现在跑 `SHOW INDEX FROM defense_records;`？（决定 N1 与 upsert 的改法）
4. **批次 C 是否接受一次性合并改动**？（省多轮重启实测）
5. **资源加固（三，现有）是否仍挂账**？答辩若要多开（教师端 + 学生端），建议至少做 JVM `-Xmx512m`。

---

# 附录 · 现场排查备忘

**1. 真机验证前**：把 `utils/config.js` 的 `useRemoteServer` 改为 `true`（当前 `false` 走 localhost）。
真机若连不上，按此顺序排查：手机与电脑连同一 WiFi → 放行 8080 入站 → 改用手机热点后重新 `ipconfig` 更新 IP。

**2. Redis 排查**：`127.0.0.1:6379`（密码 `123456`，库 0）。本机该实例**被多个项目共用**，`KEYS *` 会混入 `dict_type_one:*`、`menu_route:*`、`sys_dict:*`、`cache:shop:*`、`seckill:stock:*` 等无关键。本项目相关键只有三类：

| 键 | 用途 | TTL |
|---|---|---|
| `chat:memory:<sessionId>` | 答辩会话记忆（`RedisChatMemory`，前缀 `chat:memory:`） | **30 分钟**（`ChatConfiguration.java:43`） |
| `login:token:<uuid>` | 登录 token | 120 分钟（`app.auth.token-ttl-minutes`） |
| `upload:video:session:<uploadId>` | 视频分片续传记录 | — |

> 注意：答辩记忆 TTL 只有 30 分钟，**超过 30 分钟就查不到当场会话了**，但每轮原始回复在 `logs/ai-helper.log` 里都有；成绩不受影响（已落 MySQL）。

**3. 定位某场答辩**：日志按 `defenseId` / `sessionId` 过滤，关键行有 `收到聊天请求`、`=== AI 原始返回内容:`、`切题判定为跑题，强制五维归零`、`学生放弃/敷衍作答`、`答辩收尾完成`。
