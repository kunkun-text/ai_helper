<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ai_helper.ai_helper.mapper.PasswordResetMapper">
    <update id="updatePassword">
        update users set password = #{password} where user_number = #{userNumber}
    </update>
    <select id="selectByEmail" resultType="com.ai_helper.ai_helper.pojo.dto.UserDto">
        select user_id, user_number from users where email = #{email}
    </select>
</mapper>